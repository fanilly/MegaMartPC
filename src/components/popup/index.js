/**
 * popup 【slot】
 * top 顶部内容（仅在position != 'center'中生效）
 * bottom  底部内容（仅在position != 'center'中生效）
 *
 * popup 【props】
 * position            显示位置 允许的值 bottom, center, left, right bottom
 * height              高度
 * width               宽度
 * close-on-masker     是否点击遮罩关闭弹窗
 * masker-opacity      遮罩层透明度
 */
import Popup from './src/popup.vue';
export {Popup};
